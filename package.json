{
  "name": "unittest",
  "version": "1.0.0",
  "description": "unit tetsing using mocha and chai",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "test-mocha": "nyc mocha",
    "coverage": "nyc --reporter=html --reporter=text mocha",
    "test-istanbul": "cross-env NODE_ENV=test nyc --reporter=lcov --reporter=text mocha test/*.js"
  },
  "author": "aki",
  "license": "ISC",
  "dependencies": {
    "babel-plugin-istanbul": "^5.2.0",
    "chai": "^4.2.0",
    "cross-env": "^6.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "mocha": "^6.2.2",
    "nodemon": "^2.0.1",
    "nyc": "^14.1.1",
    "request": "^2.88.0"
  }
}
